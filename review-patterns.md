# レビューパターン集

過去のレビューから抽出した、レビュー時に注意すべき一般的なパターン。
プロジェクト固有のルールは、このファイルに追記して蓄積していく。

## 設計・責務分離

- UI / ドメインロジック / インフラ（I/O）の境界が曖昧になっていないか
- 1ファイル・1関数の責務が大きくなりすぎていないか
- 公開 API（export される関数・型）は必要最小限か

## エラーハンドリング

- 例外・エラーを握り潰していないか（空 catch、無視された戻り値）
- ユーザーに見せるエラーと内部ログで残すエラーが区別されているか
- リトライ・タイムアウト・フォールバックの方針が妥当か

## リソース管理

- ファイル / コネクション / サブスクリプション / リスナー / タイマーのクリーンアップ漏れ
- 非同期処理のキャンセル・中断パスが用意されているか

## 並行性・競合

- 共有状態への同時アクセスに対する排他制御
- 非同期処理の順序依存・競合状態
- 冪等性が必要な処理で担保されているか

## パフォーマンス

- 不必要なループ / N+1 / 過剰なコピー
- 計測なしの早すぎる最適化になっていないか
- キャッシュ・メモ化は無効化条件まで考慮されているか

## セキュリティ

- シークレット・認証情報がコードや履歴に混入していないか
- 外部入力（ユーザー / API / ファイル）のバリデーション
- インジェクション（SQL / コマンド / テンプレート / XSS 等）対策
- 認可チェックの抜け漏れ

## 型・静的検査

- 静的型付け言語で曖昧な型（`any` / `interface{}` 等）を使っていないか
- 型アサーションより型ガード・パターンマッチを優先できないか
- null / undefined / optional の扱いが明示されているか

## 命名・可読性

- 変数・関数・ファイル名が意図を表しているか
- boolean は `is` / `has` / `should` などのプレフィックスを使っているか
- マジックナンバー・マジックストリングが残っていないか
- 不要な import / 未使用コードが残っていないか

## テスト

- 変更に対応するテストが追加・更新されているか
- 境界条件・異常系がカバーされているか
- テストが実装詳細に密結合しすぎていないか

## レビューチェックリスト

- [ ] 責務分離・設計意図が明確か
- [ ] エラーハンドリング / リソース解放が適切か
- [ ] 並行性・競合の考慮が十分か
- [ ] セキュリティ上の懸念がないか
- [ ] 静的検査（lint / type check）が通るか
- [ ] 必要なテストが揃っているか
- [ ] ビルド・検証コマンドが通るか
